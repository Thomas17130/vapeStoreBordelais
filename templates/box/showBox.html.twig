<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="{{ asset('assets/css/scrollBar.css') }}">
    <link rel="stylesheet" href="{{ asset('assets/css/showBox.css') }}">

    <title>Document</title>
</head>
<body>
    <h2>Box en Stock</h2>
    <div>
        <img src="{{ asset('uploadDirectory/' ~ box.img) }}" alt="SEO à completer">
        <p>
            {{ box.brand }}
        </p>
        <p>
            {{ box.price }} euros
        </p>
    </div>
</body>
</html>